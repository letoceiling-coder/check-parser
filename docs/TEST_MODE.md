# Режим тестирования чеков

Документация по режиму тестирования для отладки и проверки работы парсера чеков.

---

## Назначение

Режим тестирования позволяет:
- Быстро проверить работу парсера на реальных чеках
- Получить детальную информацию о результате обработки
- Проверить уникальность чека в базе
- Тестировать разные методы парсинга (классический, улучшенный, интеллектуальный)
- Отладить работу без регистрации в розыгрыше

**Важно:** Режим тестирования **не сохраняет** чеки в базу данных и **не создаёт** билеты/номерки. Он только анализирует и возвращает результат.

---

## Команды

### `/test` - Вход в режим тестирования

Активирует режим тестирования для текущего пользователя.

**Пример использования:**
```
Пользователь: /test

Бот: 🧪 Режим тестирования активирован

Отправьте PDF-чек для анализа.

Вы получите:
• Дату операции
• Сумму платежа
• Уверенность распознавания (confidence)
• Метод извлечения (pdf/ocr/ai)
• Банк (если определён)
• Проверку уникальности

Команды:
/check-reset - сбросить проверку уникальности
/exit - выйти из режима тестирования
```

После активации бот ожидает отправки PDF-файлов и обрабатывает их в тестовом режиме.

---

### `/exit` - Выход из режима тестирования

Выход из режима тестирования и возврат в обычный режим работы бота.

**Пример использования:**
```
Пользователь: /exit

Бот: ✅ Режим тестирования завершён.

Отправьте /start для начала работы.
```

После выхода пользователь возвращается в состояние `IDLE` и может использовать обычные команды бота.

---

### `/check-reset` - Сброс проверки уникальности

Временно отключает проверку дубликатов для **следующего** отправленного чека.

**Использование:** Только в режиме тестирования.

**Пример:**
```
Пользователь: /check-reset

Бот: ✅ Проверка уникальности сброшена.

Следующий чек будет обработан без проверки дубликатов.
```

После обработки одного чека флаг автоматически сбрасывается и следующие чеки снова проверяются на уникальность.

**Зачем это нужно:** При тестировании одного и того же чека несколько раз, бот будет находить дубликат. Команда `/check-reset` позволяет протестировать чек повторно, игнорируя проверку на дубликаты.

---

## Формат ответа при обработке чека

После отправки PDF-файла в режиме тестирования бот возвращает детальный анализ:

```
🧪 Результат анализа чека

📄 Файл: receipt.pdf
⚙️ Метод парсинга: Улучшенный

📅 Дата операции: 25.01.2026 12:02
💰 Сумма: 10 000.00 ₽
🎯 Уверенность: ✅ 92%
🔍 Метод извлечения: Текстовый PDF (pdftotext)
🏦 Банк: Газпромбанк

🔄 Проверка уникальности:
✅ Чек уникален

Команды:
/check-reset - сбросить проверку уникальности
/exit - выйти из режима тестирования
```

### Расшифровка полей

| Поле | Описание |
|------|----------|
| **Файл** | Имя загруженного PDF-файла |
| **Метод парсинга** | Текущий метод из настроек бота: Классический / Улучшенный / Интеллектуальный (AI) |
| **Дата операции** | Извлечённая дата платежа (формат: DD.MM.YYYY или DD.MM.YYYY HH:MM) |
| **Сумма** | Извлечённая сумма платежа в рублях |
| **Уверенность** | Confidence score (0-100%): ✅ ≥90%, ⚠️ 70-89%, ❌ <70% |
| **Метод извлечения** | Источник данных: Текстовый PDF / OCR / AI / Гибрид |
| **Банк** | Определённый банк (если распознан): Сбербанк, Тинькофф, Альфа и др. |
| **Проверка уникальности** | Найден ли такой же чек в базе (по сумме + дате) |

### Возможные методы извлечения

- **Текстовый PDF (pdftotext)** — данные извлечены напрямую из текстового PDF без OCR (самый точный метод)
- **OCR** — распознавание через Tesseract, OCR.space или Google Vision
  - С указанием конкретного OCR-движка (Remote Tesseract, Local Tesseract и т.д.)
- **AI (LLM)** — извлечение через языковую модель (используется как fallback при низкой уверенности)
- **Гибрид (Enhanced + AI)** — комбинация улучшенного парсера и AI

---

## Примеры использования

### Базовый тест чека

```
1. Пользователь: /test
2. Бот: Режим тестирования активирован
3. Пользователь: [отправляет receipt.pdf]
4. Бот: ⏳ Обрабатываю чек...
5. Бот: [возвращает детальный анализ]
6. Пользователь: /exit
7. Бот: Режим тестирования завершён
```

### Тест с повторной обработкой того же чека

```
1. Пользователь: /test
2. Пользователь: [отправляет receipt.pdf]
3. Бот: [анализ чека, ✅ Чек уникален]
4. Пользователь: [отправляет receipt.pdf снова]
5. Бот: [анализ чека, ❌ Дубликат найден!]
6. Пользователь: /check-reset
7. Бот: Проверка уникальности сброшена
8. Пользователь: [отправляет receipt.pdf снова]
9. Бот: [анализ чека, ✅ Чек уникален]
```

### Тест разных методов парсинга

Чтобы протестировать разные методы, измените `receipt_parser_method` в настройках бота через админку:

1. Зайти в админку → Bot Settings
2. Выбрать метод: Классический / Улучшенный / Интеллектуальный (AI)
3. Сохранить
4. В боте: `/test` → отправить чек
5. Сравнить результаты разных методов

---

## Технические детали

### Состояние FSM

При активации `/test` пользователю устанавливается:
- `fsm_state = 'TEST_MODE'`
- `fsm_data = { skip_duplicate_check: false }`

### Обработка PDF

В режиме тестирования:
1. PDF сохраняется во временную папку `storage/app/test_mode/`
2. Обрабатывается через `processCheckWithOCR()` с текущим методом парсинга
3. Проверяется на дубликаты в таблице `checks` (по `sum` + `date` + `telegram_bot_id` + `status = approved`)
4. Файл удаляется после обработки
5. Результат **не сохраняется** в базу

### Приоритет команд

Команды `/test`, `/exit`, `/check-reset` обрабатываются **до** основной логики бота, поэтому режим тестирования работает независимо от режима розыгрыша.

### Совместимость

Режим тестирования совместим со всеми методами парсинга:
- ✅ Классический (legacy)
- ✅ Улучшенный (enhanced)
- ✅ Интеллектуальный (enhanced_ai)

И со всеми источниками текста:
- ✅ pdftotext (текстовый PDF)
- ✅ Remote Tesseract
- ✅ Local Tesseract
- ✅ OCR.space
- ✅ Google Vision
- ✅ AI fallback (при методе enhanced_ai)

---

## Логирование

Все операции в режиме тестирования логируются:

```php
Log::info('Test mode activated', ['bot_id' => ..., 'user_id' => ..., 'chat_id' => ...]);
Log::info('Test mode: PDF processed', [
    'bot_id' => ...,
    'file_name' => 'receipt.pdf',
    'amount' => 10000.00,
    'date' => '2026-01-25',
    'confidence' => 0.92,
    'source' => 'pdf',
    'is_duplicate' => false,
]);
Log::info('Test mode deactivated', [...]);
```

Ошибки также логируются:
```php
Log::error('Test mode: PDF processing error', [
    'error' => '...',
    'trace' => '...',
]);
```

---

## Ограничения

1. **Не сохраняет чеки в базу** — режим только для анализа, данные не записываются.
2. **Не создаёт билеты** — пользователь не участвует в розыгрыше при отправке чеков в test mode.
3. **Принимаются только PDF** — фотографии и другие форматы отклоняются.
4. **Проверка уникальности** — работает только среди чеков со статусом `approved` из основного режима.

---

## Отладка и диагностика

### Если чек не распознаётся

1. Проверить качество PDF (текстовый или скан)
2. Попробовать другой метод парсинга (enhanced вместо legacy)
3. Посмотреть логи: `storage/logs/laravel.log`
4. Проверить, что на сервере установлен `pdftotext` (для текстовых PDF)

### Если низкая уверенность (confidence < 0.7)

1. Переключиться на метод "Интеллектуальный (AI)" — он использует LLM как fallback
2. Проверить качество OCR (возможно, плохое качество скана)
3. Убедиться, что чек содержит ключевые слова: "итого", "сумма", "оплачено", "дата операции"

### Если находит дубликат, хотя это первая отправка

Возможно, такой чек уже был обработан в основном режиме и сохранён в базу. Используйте `/check-reset`, чтобы игнорировать проверку.

---

## Заключение

Режим тестирования — мощный инструмент для отладки парсера чеков. Используйте его для:
- Проверки новых чеков перед внедрением
- Сравнения методов парсинга
- Отладки проблем распознавания
- Демонстрации работы системы

Режим **не влияет** на основную работу бота и может использоваться в любое время без риска "сломать" розыгрыш или данные пользователей.
